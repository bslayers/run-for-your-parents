using UnityEngine.Events;
using UnityEngine;

public class InteractionEvents : MonoBehaviour
{   
    [Tooltip("Event triggered when the object is hovered over.")]
    public UnityEvent OnInteract;
}
